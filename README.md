# TBA-Fan-Network

Run locally with:

```bash
npm install
npm run dev
```

Open http://localhost:3000. Updates from the commissioner dashboard are now shared across users because the app uses a common server-backed state file.